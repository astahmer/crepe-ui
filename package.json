{
  "name": "pacha",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter=./packages/preset-chakra dev",
    "build": "pnpm -r --filter=./packages/** build",
    "fmt": "prettier --write ./'packages/*/src/**/*.{ts,tsx}'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
